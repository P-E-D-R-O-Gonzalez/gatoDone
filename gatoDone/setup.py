from setuptools import setup
setuptools.setup(     
     name="gatoDone",     
     version="0.0.1",
     packages=["gatoDone"],
     install_requires["playsound"],
     package_dat={"gatoDone":["*.mp3"]},
)
